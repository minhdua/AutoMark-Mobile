<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.vothanhhien.automarkmobile">
    <dist:module dist:instant="true" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.NhapDapAn.Hand.MakeActivity"
            android:label="Đáp án cho đề 1234"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.PhieuTraLoi.DsPhieuTraLoi"
            android:label="Danh sách bài làm"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.DapAn.DanhSachDapAn"
            android:label="Danh sách đáp án"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.ViewResultScore"
            android:label="Xem kết quả"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.Main.MainActivity"
            android:label="Danh sách bài thi"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity android:name=".activities.SettingsActivity">-->
<!--            -->
<!--        </activity>-->
        <activity android:name=".activities.TuyChon.ChamDiemActivity"
            android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>